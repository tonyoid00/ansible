---

### all bootstrap
  - name: Initialize nodes
    hosts: "{{ hostname }}"
    become: yes
    roles:
      - {role: apt-ugyfelhivo, tags: packages}
      - {role: bootstrap, tags: bootstrap}
     # - {role: users-groups, tags: users}
      - {role: custom-ugyfelhivo, tags: custom}
      - {role: time, tags: time}
      - {role: wireguard, tags: wg}
      - {role: adauth, tags: ad}
