---

### all bootstrap
  - name: Initialize nodes
    hosts: ugyfelhivo1
    become: yes
    roles:
      #- {role: swapfile, swapfile_size: 1GB, tags: [swapfile] }
      - {role: bootstrap, tags: bootstrap}
      #- {role: users-groups, tags: users}
      #- {role: sudoers, tags: sudoers}
      #- {role: ssh, tags: ssh}
      #- {role: hosts, tags: hosts}
      #- {role: iptables, tags: iptables}
      #- {role: openfiles, tags: openfiles}
      #- {role: network-tuning, tags: network}
      - {role: custom, tags: custom}
      #- {role: check-mk-agent, tags: check-mk}

