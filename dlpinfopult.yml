---

### all bootstrap
  - name: Initialize nodes
    hosts: "{{ hostname }}"
    become: yes
    roles:
      - {role: wireguard, tags: wg}
      - {role: apt-dlpinfopult, tags: packages}
      - {role: bootstrap, tags: bootstrap}
      - {role: custom-dlpinfopult, tags: custom}
      - {role: time, tags: time}
      - {role: adauth, tags: ad}
